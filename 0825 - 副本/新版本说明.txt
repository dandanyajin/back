
 #老版本是固定数据，登录人的名字及下拉中的具体页面没有

 #新版本是请求回来的数据，登录人的名字及下拉中的
    具体页面有，并要求在iframe中打开，由于地址与侧导航
    的地址拼接不同，且相应数据不跟侧导航的数据在一块。
    所以重新增加了一个iframe。所以top切换增加很多的判断。
    
 #新版本是前后端不分离，数据格式会和老版本不同
 
 #新版本中引入了jquery(用来请求数据。因为之前后台管理框架用jquery写的不能用了，才叫我重新写个，我就用vue.js写的，最后衔接就这样了)
